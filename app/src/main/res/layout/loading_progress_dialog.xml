<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:id="@+id/load_layout"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#00000000"
                android:layout_gravity="center">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="45dp"
        android:background="@drawable/shape_loading_progress"
        android:gravity="center"
        android:layout_centerInParent="true"
        android:padding="10dp"
        android:orientation="horizontal">

        <ProgressBar
            android:id="@+id/iv_loading_progress_dialog_loadprogress"
            style="?android:attr/progressBarStyleSmall"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_gravity="center"
            android:layout_marginRight="4dp"
            android:indeterminateDrawable="@drawable/drawable_loading_progress"/>

        <TextView
            android:id="@+id/more_data_msg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/loading_progress"
            android:textColor="@color/white"
            android:singleLine="true"
            android:textSize="@dimen/text16_size" />
    </LinearLayout>
</RelativeLayout>